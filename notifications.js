// File: notifications.js
// This script can be used for future interactivity on the notifications page.

document.addEventListener('DOMContentLoaded', () => {
    // The "plus" button on this page's nav bar is an <a> tag,
    // so it doesn't need the pop-up menu logic.
    // This file is ready for future logic, such as "Mark all as read".

    console.log("Notifications page loaded.");
});
